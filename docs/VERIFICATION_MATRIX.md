# Verification Matrix (Phase-4, current v11 series)

Introduced in M139; maintained through current Phase-4 milestones.

This matrix links reviewer-visible requirements to executable checks and
artifacts already present in the repository.

| Requirement / claim boundary | Script or test | Input(s) | Output artifact(s) | Acceptance check |
|---|---|---|---|---|
| Repository contract files are present | `scripts/phase2_repo_inventory.py` and `tests/test_phase2_m108_repo_inventory_require_present.py` | `` tree | inventory JSON/text | exit 0 with `--require-present`; required files have `exists=true` and 64-char SHA256 |
| Canonical roadmap files are reviewer-visible | `scripts/phase2_repo_inventory.py`, `tests/test_phase2_m108_repo_inventory_require_present.py`, `tests/test_phase4_m139_docs_claims_lint_and_required_docs_present.py` | `docs/GSC_Consolidated_Roadmap_v2.8.md`, `docs/GSC_Consolidated_Roadmap_v2.8.1_patch.md` | inventory JSON/text + docs checks | both files exist; inventory marks them required and present |
| Task 4A.-1 SigmaTensor drift-sign supporting no-go diagnostic is deterministic and schema-validated | `scripts/phase4_sigmatensor_drift_sign_diagnostic.py`, `tests/test_phase4_m145_drift_sign_diagnostic_determinism_toy.py`, `tests/test_phase4_m145_schema_validate_auto_toy.py` | fixed lambda/z grid + fixed `created_utc` | `DRIFT_SIGN_DIAGNOSTIC.json`, `DRIFT_SIGN_DIAGNOSTIC.md` | script exits 0; JSON schema validates via `phase2_schema_validate.py --auto`; repeated runs are byte-identical for JSON/MD |
| Task 4A.-0 repurposed no-go gap quantification is deterministic and schema-validated | `scripts/phase4_sigmatensor_optimal_control_gap_diagnostic.py`, `tests/test_phase4_m146_gap_diagnostic_determinism_toy.py`, `tests/test_phase4_m146_schema_validate_auto_toy.py` | fixed lambda/z control grid + fixed `created_utc` | `GAP_DIAGNOSTIC.json`, `GAP_DIAGNOSTIC.txt` | script exits 0; schema validates via `phase2_schema_validate.py --auto`; repeated runs are byte-identical for JSON/TXT |
| Task 4A.9 epsilon-framework readiness audit is deterministic and git-less compatible | `scripts/phase4_epsilon_framework_readiness_audit.py`, `tests/test_phase4_m147_epsilon_audit_determinism_toy.py`, `tests/test_phase4_m147_schema_validate_auto_toy.py`, `tests/test_phase4_m147_epsilon_audit_gitless_snapshot_toy.py` | repo snapshot (with and without `.git`) + fixed `created_utc` | `EPSILON_FRAMEWORK_READINESS_AUDIT.json`, `EPSILON_FRAMEWORK_READINESS_AUDIT.md` | script exits 0; schema validates via `phase2_schema_validate.py --auto`; repeated runs are byte-identical; git-less snapshot run still emits valid report |
| Task 4B.1 epsilon translator MVP emits deterministic schema-validated artifacts | `scripts/phase4_epsilon_translator_mvp.py`, `tests/test_phase4_m148_epsilon_translator_determinism_toy.py`, `tests/test_phase4_m148_schema_validate_auto_toy.py`, `tests/test_phase4_m148_epsilon_translator_gitless_snapshot_toy.py` | fixed epsilon toy inputs + fixed `created_utc`, repo snapshot (with and without `.git`) | `EPSILON_TRANSLATOR_MVP.json`, `EPSILON_TRANSLATOR_MVP.md` | script exits 0; schema auto-validation exits 0; repeated deterministic runs are byte-identical; git-less snapshot run emits portable report |
| Task 4B.2 epsilon sensitivity matrix toy is deterministic and self-verified | `scripts/phase4_epsilon_sensitivity_matrix_toy.py`, `tests/test_phase4_m149_epsilon_sensitivity_determinism_toy.py`, `tests/test_phase4_m149_schema_validate_auto_toy.py`, `tests/test_phase4_m149_epsilon_sensitivity_gitless_snapshot_toy.py` | fixed epsilon vector + fixed pivot z values/exponents + fixed `created_utc`, repo snapshot (with and without `.git`) | `EPSILON_SENSITIVITY_MATRIX_TOY.json`, `EPSILON_SENSITIVITY_MATRIX_TOY.md` | script exits 0; `self_check.self_check_ok=true`; schema auto-validation exits 0; repeated deterministic runs are byte-identical; git-less snapshot run emits portable report |
| Task 4B.3 Pantheon+ epsilon posterior paper-grade plotting path (M155) is deterministic, schema-validated, and git-less compatible | `scripts/phase4_pantheon_plus_epsilon_posterior.py`, `scripts/fetch_pantheon_plus_release.py`, `tests/test_phase4_m150_pantheon_epsilon_posterior_determinism_toy.py`, `tests/test_phase4_m154_pantheon_epsilon_posterior_determinism_toy.py`, `tests/test_phase4_m155_pantheon_paper_grade_requires_manifest_toy.py`, `tests/test_phase4_m155_pantheon_paper_grade_requires_full_cov_toy.py` | fixed deterministic config (`--deterministic 1`) with committed toy full-cov fixture + toy manifest in tests; repo snapshot (with and without `.git`) | `PANTHEON_EPSILON_POSTERIOR_REPORT.json`, `PANTHEON_EPSILON_POSTERIOR_REPORT.md`, `epsilon_posterior_1d.png`, `omega_m_vs_epsilon.png` | script exits 0 in demo/full-cov toy mode; schema auto-validation exits 0 (`phase4_pantheon_plus_epsilon_posterior_report_v2`); deterministic runs are byte-identical including PNGs; paper-grade mode fails fast when manifest/full-cov requirements are missing |
| Task 4B.4 Triangle-1 BAO baseline leg (M156) is deterministic, schema-validated, and git-less compatible | `scripts/fetch_desi_bao_products.py`, `scripts/phase4_desi_bao_epsilon_or_rd_diagnostic.py`, `tests/test_phase4_m156_desi_bao_diagnostic_determinism_toy.py`, `tests/test_phase4_m156_schema_validate_auto_toy.py`, `tests/test_phase4_m156_desi_bao_diagnostic_gitless_snapshot_toy.py` | fixed deterministic config (`--deterministic 1`) with compact DR1 baseline table and optional pinned fetch manifest; repo snapshot (with and without `.git`) | `DESI_BAO_TRIANGLE1_REPORT.json`, `DESI_BAO_TRIANGLE1_REPORT.md`, `epsilon_posterior_1d.png`, `omega_m_vs_epsilon.png` | fetch manifest emits pinned SHA256 rows (`phase4_desi_bao_fetch_manifest_v1`); BAO diagnostic schema auto-validation exits 0 (`phase4_desi_bao_triangle1_report_v1`); deterministic runs are byte-identical including PNGs; git-less snapshot run emits portable report |
| Task 4B.5 Triangle-1 joint SN+BAO+Planck acoustic-scale closure (M157) is deterministic, schema-validated, and git-less compatible | `scripts/phase4_triangle1_sn_bao_planck_thetastar.py`, `scripts/phase4_desi_bao_convert_gaussian_to_internal.py`, `tests/test_phase4_m157_triangle1_determinism_toy.py`, `tests/test_phase4_m157_schema_validate_auto_toy.py`, `tests/test_phase4_m157_triangle1_joint_sn_bao_gitless_snapshot_toy.py` | fixed deterministic config (`--deterministic 1`) with toy fixtures for CI; paper-grade mode uses full-cov Pantheon inputs + converted DESI DR1 Gaussian dataset + compressed Planck `lA` prior | `TRIANGLE1_SN_BAO_PLANCK_REPORT.json`, `TRIANGLE1_SN_BAO_PLANCK_REPORT.md`, `epsilon_posterior_1d.png`, `omega_m_vs_epsilon.png` | schema auto-validation exits 0 (`phase4_triangle1_report_v1`); report explicitly records `r_d≈r_s` closure assumption and compressed-Planck scope; deterministic reruns are byte-identical including PNGs; git-less snapshot run emits portable report |
| M158 Paper-2 publish assets builder emits deterministic figures + numbers + schema-valid manifest | `scripts/phase4_build_paper2_assets.py`, `tests/test_phase4_publish_paper2_assets_ci_smoke_toy.py`, `tests/test_phase4_publish_schema_validate_auto_toy.py` | fixed deterministic config (`--preset ci_smoke --seed 0`) with repo toy fixtures only | `paper2_assets_manifest.json`, `numbers.tex`, `figures/fig_*.png` | script exits 0; manifest validates (`phase4_paper2_assets_manifest_v1`); reruns are byte-identical |
| M160 QCD<->Gravity bridge annex artifact generator is deterministic and reviewer-safe (idea-bank only) | `bridges/phase4_qcd_gravity_bridge_v0.1/tools/make_qcd_gravity_bridge_artifacts.py`, `tests/test_phase4_m160_qcd_gravity_bridge_artifacts_deterministic.py` | fixed deterministic config (`--preset ci_smoke --seed 0 --emit-plot 1`) | `qcd_gravity_bridge_numbers.json`, `qcd_gravity_bridge_kill_matrix.csv`, `qcd_gravity_bridge_scale_plot.png` | deterministic run matches committed golden SHA256 outputs; report contains explicit non-claim guardrails and `paths_redacted=true` |
| M160 optional Paper-2 assets annex wiring copies bridge artifacts without changing primary claims | `scripts/phase4_build_paper2_assets.py` | `--include-theory-annex` with standard CI-smoke inputs | `paper2_assets_manifest.json`, `theory/qcd_gravity_bridge/*` | build exits 0, manifest remains schema-valid, and supplementary theory files are present only when flag is enabled |
| Publication/outreach branding pack is present, non-empty, and keeps mandatory transparency wording | `tests/test_phase4_publish_branding_pack_present_and_nonempty.py`, `scripts/phase2_repo_inventory.py` | repository tree only (offline) | `docs/AFFILIATION_AND_BRANDING.md`, `outreach/labs_site_copy/*.md`, `outreach/templates/*.md` | test exits 0; outreach files are present and non-empty; transparency copy contains `non-claims`, `μ-running ≠ time variation`, and `white-hat` |
| M158 Paper-2 arXiv bundle tooling is deterministic and arXiv-friendly | `scripts/build_paper2.sh`, `scripts/phase4_make_arxiv_bundle_paper2.py` | Paper-2 source tree + generated assets directory | `paper_assets/paper2_measurement_model_epsilon.pdf`, `paper_assets/paper2_arxiv_bundle.tar.gz` | bundle command exits 0 and prints deterministic member/file-size summary |
| M158 JOSS preflight emits deterministic machine-readable report and checks submission prerequisites | `scripts/phase4_joss_preflight.py`, `tests/test_phase4_publish_joss_preflight_determinism_toy.py` | repository root files (`LICENSE`, `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `paper.md`, `paper.bib`, `CITATION.cff`) | JSON report payload (`phase4_joss_preflight_report_v1`) | exit 0 when metadata exists and required headings/keys are present; output is deterministic across reruns |
| M159 submission-pack docs/metadata are ready for UI upload without extra preprocessing | `scripts/phase2_repo_inventory.py`, `tests/test_phase2_m108_repo_inventory_require_present.py` | `docs/ARXIV_METADATA.md`, `docs/ARXIV_UPLOAD_CHECKLIST.md`, `docs/PAPER2_SUBMISSION_GUIDE.md`, `docs/JOSS_AUTHORS.md`, `docs/JOSS_SUBMISSION_GUIDE.md` | inventory JSON/text + docs files | required docs exist and are marked `required=true` with valid SHA256 in inventory output |
| M153 narrative sync guard prevents reintroducing drift-sign as primary (historical/supporting-only outside legacy context) | `tests/test_phase4_m153_no_primary_drift_outside_legacy.py` | canonical entrypoint docs + paper-readiness lint script text | unittest assertion log | no forbidden `drift + primary` or legacy-primary framing tokens in scoped non-legacy files; legacy allowlist remains explicit |
| Reviewer-facing claims stay within scope guardrails | `scripts/docs_claims_lint.py` | canonical docs set | lint findings (stdout) | exit 0; no banned overclaim findings |
| Legacy `v10*` filename hygiene remains bounded to approved provenance zones | `tests/test_phase4_m152_legacy_versioned_filenames_bounded.py` | repository tree walk (git-less compatible) | unittest assertion log | all `v10*` paths match `LEGACY_VERSIONED_ARTIFACTS.md` allowlist; root-level wrappers keep `HISTORICAL` + `DO NOT SUBMIT` banners |
| Red-team adversarial regression checks stay green and share-safe | `scripts/phase4_red_team_check.py`, `tests/test_phase4_m141_red_team_check_emits_deterministic_report_toy.py` | `` tree and transient share snapshot | `RED_TEAM_REPORT.json`, `RED_TEAM_REPORT.md` | exit 0 in strict mode; report marks `paths_redacted=true`; snapshot preflight block has forbidden matches = 0 and size budget passes |
| CosmoFalsify golden demo runs end-to-end in git-less snapshots | `scripts/phase4_cosmofalsify_demo.py`, `tests/test_phase4_m142_cosmofalsify_demo_determinism_toy.py`, `tests/test_phase4_m142_schema_validate_auto_toy.py` | toy scan settings + fixed `created_utc` | `cosmofalsify_demo_report.json`, `cosmofalsify_demo_report.md`, `artifacts/bundle.zip` | report schema validates via `phase2_schema_validate.py --auto`; stage statuses all `ok`; outputs are deterministic across repeated runs |
| Snapshot sharing is deterministic and portable | `scripts/make_repo_snapshot.py`, `scripts/preflight_share_check.py`, `tests/test_phase2_m116_make_repo_snapshot_review_with_data_profile.py` | git-tracked repository state | zip + manifest + preflight report | deterministic zip hash for same input; forbidden path count is zero |
| Schema-based artifact validation is operational | `scripts/phase2_schema_validate.py` (auto mode), `tests/test_phase2_m117_schema_validate_auto_selects_schema_toy.py` | JSON artifact with `schema` field | validation result | known schema IDs validate with exit 0; unknown schema IDs fail with exit 2 |
| Portable-content leaks are detected in JSON/JSONL content | `scripts/phase2_portable_content_lint.py`, `tests/test_phase2_m117_portable_content_lint_detects_absolute_paths_toy.py` | dir/zip with JSON/JSONL | lint report | exit 2 when machine-local absolute-path tokens are present |
| Phase-3 low-z scan plan/run is deterministic | `scripts/phase3_scan_sigmatensor_lowz_joint.py`, `tests/test_phase3_m130_lowz_scan_plan_determinism_toy.py`, `tests/test_phase3_m130_lowz_scan_jsonl_determinism_toy.py` | fixed CLI params + fixed `created_utc` | plan JSON + scan JSONL | identical bytes across repeated runs |
| Single-slice job packs are merge-safe (M138) | `scripts/phase3_lowz_jobgen.py`, `tests/test_phase3_m138_lowz_jobgen_slices_1_merge_smoke_toy.py`, `tests/test_phase3_m138_phase2_merge_accepts_single_jsonl_toy.py` | `--slices 1` toy pack + one shard | merged JSONL | run + merge complete with exit 0 and non-empty merged output |
| Candidate triage is reproducible and reviewer-portable | `scripts/phase3_analyze_sigmatensor_lowz_scan.py`, `scripts/phase3_make_sigmatensor_candidate_dossier_pack.py`, `tests/test_phase3_m132_candidate_dossier_pack_determinism_toy.py`, `tests/test_phase3_m136_dossier_quicklook_report_determinism_toy.py` | scan JSONL / analysis JSON | analysis outputs + dossier manifest + quicklook | deterministic outputs; portable-content lint gate passes |
| CLASS-export mapping consistency is quantified (diagnostic) | `scripts/phase3_pt_sigmatensor_class_mapping_report.py`, `tests/test_phase3_m135_class_mapping_report_zero_residuals_on_self_consistent_toy.py` | class export pack | mapping report JSON/MD | self-consistent toy case yields near-zero residuals |
| Spectra files from external runs get basic sanity checks | `scripts/phase3_pt_spectra_sanity_report.py`, `tests/test_phase3_m126_spectra_sanity_report_detects_tt_from_class_cl_dat_toy.py` | run dir or results pack | spectra sanity report | TT detection and gate behavior match test expectations |

Note on terminology:
`k(sigma)` identification language in this repository is treated as an ansatz
and open problem unless a derivation-level artifact is explicitly provided.

## Reviewer note

The matrix is intentionally operational: each row points to a reproducible
script/test path and an observable acceptance condition.
